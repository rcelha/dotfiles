#!/bin/sh

if [ ! `which stow` ]; then
    echo Install GNU stow
    echo exit; exit 1
fi;

mkdir -p ~/.vim
mkdir -p ~/.ssh/config.d

stow -t ~ general
stow -t ~ sources
stow -t ~ tmux
stow -t ~/ -d vim vimrc
stow -t ~/.vim -d vim vim
stow -t ~ zsh
stow -t ~/.ssh ssh
